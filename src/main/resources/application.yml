spring:

  profiles:
    include: secret

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ENC(yzHn3XyO+t2v60Sqngbd3nepAzMx1WHbkSXu4zDg3ngSXCSxqtkZLw8hQcc27M3f)
    username: ENC(NIHDYPZNJyk6Uqd/8mUA0g==)
    password: ENC(nwTJxOXNNpHR00EjOcipwg==)

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      properties:
        hibernate.format_sql: true
      ddl-auto: validate

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  port: 8080
